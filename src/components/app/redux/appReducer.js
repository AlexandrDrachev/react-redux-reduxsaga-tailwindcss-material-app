

export const initialAppState = {
    realData: false,
    user: "user",
    loading: true,
    error: false,
    messageError: null
};

export const appReducer = (state, action) => {

    return state;
};